# Database
# Pour Railway, configurez cette variable dans le dashboard Railway
# Ne commitez jamais le fichier .env avec la vraie valeur
DATABASE_URL="postgresql://postgres:PASSWORD@HOST:PORT/DATABASE"

# Exemple pour Railway (remplacez par vos vraies valeurs)
# DATABASE_URL="postgresql://postgres:jgQLrQahWKPtBryEKFSMuonagOOSHGiN@turntable.proxy.rlwy.net:49842/railway"

# NextAuth Configuration
# Générez une clé secrète avec: openssl rand -base64 32
# Ou utilisez: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET="your-secret-key-here-generate-a-random-32-byte-base64-string"
NEXTAUTH_URL="http://localhost:3000"
# Pour NextAuth v5, trustHost doit être true en production ou configuré dans auth.ts
AUTH_TRUST_HOST="true"
# Pour NextAuth v5, AUTH_URL peut être utilisé à la place de NEXTAUTH_URL
# AUTH_URL="http://localhost:3000"

# Google OAuth (pour l'authentification Google)
# Obtenez ces valeurs depuis Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"


